auto-install-peers=false
strict-peer-dependencies=false

# Use hoisted node_modules so pnpm doesn't need symlink privileges on Windows.
# Symlinks require Developer Mode or admin rights on Windows; hoisting avoids that.
node-linker=hoisted
